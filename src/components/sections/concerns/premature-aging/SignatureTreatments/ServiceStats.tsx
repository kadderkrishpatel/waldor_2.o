"use client";

import useSectionReveal from "@/src/components/hooks/useSectionReveal";

export default function ServiceStats() {
  const sectionRef = useSectionReveal();

  return (
    <section
      ref={sectionRef}
      className="m-2 lg:m-4 rounded-[32px]"
    >
      
    </section>
  );
}
